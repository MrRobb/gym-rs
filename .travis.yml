sudo: required
matrix:
  include:
    - language: rust
      addons:
        apt:
          packages:
            - "python3"
            - "python3-pip"
      python:
        - 3.7
      rust:
        - stable
      install:
        - sudo pip install --upgrade pip
        - sudo pip3 install --upgrade pip
        - sudo pip3 install -r requirements.txt
      script:
        - cargo build --verbose
        - cargo test --verbose
